{% set classes = ['node'] %}
<article {{ attributes.addClass(classes) }}>
    <div id="main" class="despliegue-nodo">
        <div id="node-infographic" class="container contenido-nodo">
            <div class="left" style="background-image: url('{{ file_url(node.field_image.entity.uri.value) }}');background-repeat: no-repeat">
                <div class="box">
                    <div class="top">
                        <div class="section">
                            {{ content.field_categoria }}
                        </div>
                        <div class="buttons">
                            <div {% set nid=node.id %} class="node_save {% if nid in nodo_saved['split'] %}saved{% endif %}" data-id="{{ nid }}" data-session="{% if user.id != 0 %}authentificate{% else %}anonime{% endif %}"></div>
                        </div>
                    </div>

                    <h1 {{ attributes }}>{{ node.title.value }}</h1>

                    <div class="info article">
                        <span class="author">
                            {% for autor in node.field_autores %}
                                <b>{{ autor.entity.field_nombre.value }} {{ autor.entity.field_apellidos.value }}</b> -              
                            {% endfor %}
                        </span>
                        <date>{{ content.field_fecha.value|date('U')|format_date('global') }}</date>
                    </div>

                    <div class="statistics">
                        <label>Estadísticas</label>
                        <div class="item views">
                            <i class="icon icon-views">Vistas</i>
                            <span><b>1,909</b>Vistas</span>
                        </div>
                    </div>

                    <div class="tags">
                        {% if content.field_tags[0] %}
                        <label>Tags</label>
                        <ul>
                            {% for key, item in content.field_tags if key|first != '#' %}
                            <li>{{ item }}</li>
                            {% endfor %}
                        </ul>
                        {% endif %}
                    </div>

                    <div class="shared">
                        <label>Share</label>
                        <div class="a2a_kit a2a_default_style">
                            <a class="a2a_button_facebook">
                                <img src="\themes\custom\basic\images\redes-facebook.svg" border="0" alt="Facebook" width="27" height="27">
                            </a>
                            <a class="a2a_button_whatsapp">
                                <img src="\themes\custom\basic\images\redes-whatsapp.svg" border="0" alt="Twitter" width="27" height="27">
                            </a>
                            <a class="a2a_button_twitter">
                                <img src="\themes\custom\basic\images\redes-twitter.svg" border="0" alt="Twitter" width="27" height="27">
                            </a>
                            <a class="a2a_button_linkedin">
                                <img src="\themes\custom\basic\images\redes-linkedin.svg" border="0" alt="Share" width="27" height="27">
                            </a>
                        </div>
                    </div>
                </div>
            </div>

            <div class="right">
                <div class="content article infografia">
                    <div class="infografia">
                        {{ node.field_infografia.value|raw }}
                    </div>

                    <div class="summary">
                        <p>{{ node.body.summary }}</p>
                    </div>

                    <div class="article__body">
                        {{ node.body.value|raw }}
                    </div>
                </div>
            </div>
        </div>
        <!-- /#main -->
        <!-- /.container -->
        <div class="container">
            <div class="news infinite-contents infographics">
                <div class="block-title">
                    <h3>Más infográfias</h3>
                </div>
                {{ drupal_view('articulos', 'block_1') }}
            </div>
        </div>
        <!-- /.container -->
    </div>
</article>
<!-- /.node -->